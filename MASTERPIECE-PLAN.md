# 🏠 בית בסדר — תוכנית-מופת (4.7.2026)

> **נכתב אחרי צלילת-עומק** (בקשת-אלעד 4.7.2026, "להחיות לפי החזון המקורי").
> קרא לפני הכל: `docs/AGENT-INTERFACE.md` (החזון המקורי) · `docs/LAUNCH-READINESS-2026-06-14.md` · `PROGRESS.md`.

---

## ⚡ עדכון-ביצוע 5.7.2026 — אופק א' (48 השעות) בוצע ואומת

| צעד | סטטוס | הוכחה |
|---|---|---|
| **חיווט קאמי ↔ בית-בסדר** | ✅ חי בפרודקשן של קאמי | מודול חדש `src/integrations/bayit/bayit-client.ts` + אינטנטים ב-`message-router.ts` + הזרקה ב-`index.ts` (Contabo, `/opt/elad-personal-agent`). מבחן חי מהשרת של קאמי: **15/15 ✅** כולל סבב-כתיבה מלא (add→complete, +10 נק'). גיבויים: `/opt/backups/*.pre-bayit-20260704-231117.bak`. שער-הקבוצות לא נגוע (דיף = תוספות-בלבד, 0 שורות הוסרו). |
| ↳ ביטויים שקאמי מבין עכשיו | ✅ | "מה יש היום בבית?" · "תוסיף משימה: X" · "סיימתי את X" (עם gate-התאמה — אין התאמה ⇒ זורם ל-AI הרגיל) · "תוכנית שבועית לבית". צ'אט פרטי בלבד. לקח שנתפס: אותיות-סופיות ב-regex עברי (תוסיף = ף, לא פ). |
| ↳ סודות | ✅ מתועד | `BAYIT_AGENT_KEY` הועתק ל-env של קאמי **מהמקור הקנוני** (bayit-beseder `.env.local` / Vercel env) — לא נוצר סוד חדש. `BAYIT_HOUSEHOLD_ID` כבר היה שם. |
| **brief חומל** | ✅ בקוד, ב-branch `feat/home-launch-48h` (preview, לא master) | `buildKindOverdueLine` (0→שקט · 1-3→מספר כן+בחירה · 4+→**בלי מספר**, הצעת "התחלה נקייה") + סינון תבניות `{overdueCount}` ב-adaptive coaching. בדיקות חדשות: 20/20, כולל "43 לא מודפס באף סגנון". |
| **סקריפט התחלה-נקייה** | ✅ מוכן, **לא הורץ** | `scripts/fresh-start.mjs` — dry-run כברירת-מחדל, מארכב כ-skipped (לא מוחק). dry-run אמיתי הורץ: **43 משימות תקועות, כולן פבר'-מרץ 2026**. ההרצה ממתינה לטאפ של אלעד: `node scripts/fresh-start.mjs --cutoff 2026-07-01 --household <uuid> --apply` |
| **טיוטת הזמנת-ענבל** | ✅ טיוטה, **לא נשלחה** | `docs/LAUNCH-HOME.md` — 2 נוסחים בקול-אלעד + טקס-10-דקות + כללי-זהב. אלעד שולח בעצמו. |
| **שאלון-הארוחות (5 השאלות)** | ✅ מוכן לקאמי, **לא נשלח** | `docs/MEAL-PLANNER-INTAKE.md` — הודעת-וואטסאפ מוכנה + טיוטת-סבב לתיקון (לא לזריעה). נשלח רק כשאלעד מאשר. |
| **תשתית מתכנן-הארוחות** | ✅ קוד בלבד, DB לא נגוע | מיגרציה `supabase/migrations/015_meals.sql` (**לא הורצה**) + stub `GET /api/agent/prep` (מחזיר 501 עם החוזה המתוכנן). |
| אימות | ✅ | tsc 0 · build ירוק · 326/331 בדיקות עוברות (5 כשלונות `useTaskStreaks` **קדם-קיימים** — נכשלים גם על עץ נקי בלי השינויים; ארטיפקט סביבה). |

**3 הטאפים של אלעד עכשיו:** (1) אישור הרצת fresh-start · (2) "קאמי, תשלח לי את שאלון הארוחות" · (3) שליחת ההזמנה לענבל + טקס הערב.
**ליורש:** אימות Vercel-preview של ה-branch → מיזוג ל-master → Phase-2 לפי `docs/MEAL-PLANNER-INTAKE.md` §"מה קורה אחרי".

---

## 1. האמת במשפט אחד

**בנינו בית יפהפה — ואף אחד לא גר בו.**

האפליקציה חיה (bayitbeseder.com, בדוק 4.7: מחזירה 200), מלוטשת, launch-ready מ-14.6,
עם API לסוכנים שעובד (אומת בקריאה חיה 4.7). אבל בדיקת-האמת של היום מצאה:

| בדיקה (4.7.2026, קריאה חיה מה-API) | תוצאה |
|---|---|
| משימות פתוחות להיום | **0** |
| משימות באיחור | **43** |
| רצף (streak) | **0** |
| קאמי מחובר בפועל (מפתח + קוד בצד ה-VPS) | **לא** — 0 איזכורים, אין BAYIT_AGENT_KEY בסביבה שלו |
| מתכנן-הארוחות (הכאב המקורי) | **לא נבנה** — חסום על 5 תשובות-intake מאלעד מאז 27.5 |

זה לא כישלון של קוד. זה פער-אימוץ קלאסי: המוצר מחכה שיפתחו אותו,
והמשפחה חיה בוואטסאפ. **החזון המקורי אמר בדיוק את זה** ("שתי דלתות-כניסה —
UI וסוכן") — ודלת-הסוכן נבנתה רק מצד אחד. הבית פתח דלת; אף סוכן לא נכנס בה.

---

## 2. מה בנוי (מפה מאומתת — reuse, לא להמציא מחדש)

### חי בפרודקשן ✅
- **מוצר-זוגי מלא, חינמי לגמרי** (useSubscription מקובע `free`): משימות (8+ קטגוריות),
  מתכנן שבועי לפי אזורי-בית + מודעות-שבת, רשימת-קניות realtime, סטטיסטיקות,
  **מד-הוגנות לא-מבייש** (moat אמיתי), גיימיפיקציה (הישגים, love tokens, קופסת-הפתעה,
  גלגל-מזל שבועי), אימון-AI (Gemini), צ'אט-AI, אונבורדינג שיחתי (953 שורות), i18n מלא
  976=976 עברית/אנגלית, PWA אופליין, push, Sentry, Plausible, GEO 100/100.
- **Agent API v1.1.0** (`/api/agent/{capabilities,plan,brief,task}`): Bearer עם
  `BAYIT_AGENT_KEY`, fails-closed, rate-limited, task list/add/complete —
  **אומת חי 4.7** כולל אימות WhatsApp-delivery אמיתי ב-19.6.
- **קרונים ב-Vercel**: daily-brief 08:00 IL, daily-summary, auto-schedule, calendar-sync
  (`vercel.json`). ⚠️ לא אומת שהם באמת רצים — דורשים `CRON_SECRET` בסביבת Vercel (§5-א).
- **WhatsApp דו-כיווני**: שליחה (Green API) + webhook נכנס reply-to-complete (HMAC-מוגן).
- 325 בדיקות · tsc נקי · master==origin/master · הקשחת-auth `d3a4a57` **כן** ב-master.

### מעוצב אך לא בנוי 📋
- **מתכנן-ארוחות Phase 2** — הכאב שבגללו הפרויקט קיים (הפשרה/בישול-מראש, עייפות-החלטה,
  "רק אני אכלתי את זה"). מודל-נתונים מלא + לוגיקה + intake של 5 שאלות מוכנים
  ב-`docs/AGENT-INTERFACE.md`. חסר רק: התשובות של אלעד + מיגרציה + cron.
- **חיבור קאמי** — bridge-directive נשלח 27.5, אבל אפס חיווט בפועל בצד ה-VPS.

### חסמים פתוחים (מהם רק אחד רלוונטי לשלב-הבית) ⛔
1. ~~תשלומים~~ — `SUMIT_WEBHOOK_SECRET` חסר ב-Vercel. **לא רלוונטי עכשיו**: כל חוויית-הזוג חינמית.
   ⚠️ ובנוסף — תגלית מ-PDF-Empire (1.7): Sumit העבירו את ה-hosted-checkout ל-
   `/creditguy/gateway/`. ה-checkout של בית-בסדר יושב על אותו `_lib/sumit-client` ישן →
   **כנראה שבור גם כאן**. לתקן רק בשלב-המונטיזציה (§6, אופק-3), עם הפיקס המוכח מ-PDF-Empire.
2. מיגרציה 014 (scope משימות למשק-בית) — ה-RLS החי כבר אוכף members-only (LAUNCH-2026-06-08);
   ההרצה הפורמלית = טאפ של אלעד על branch קודם.
3. הודעת-ההזמנה לענבל (נוסחה ב-8.6 ב-`docs/LAUNCH-2026-06-08.md`) — **הקובץ אבד** (לא בריפו).
   לנסח מחדש (§5-ה).
4. קובץ-דמה מבלבל `supabase/migrations/001_initial_schema.sql` — סכימה נטושה שכבר הטעתה
   אודיט. להסגר (§5-ו).

---

## 3. המרחק מהחזון המקורי

| החזון (AGENT-INTERFACE.md, 27.5) | מצב 4.7 |
|---|---|
| לפרק את הקונפליקט הביתי סביב מטלות | ❌ המוצר לא בשימוש → הקונפליקט לא טופל |
| להוריד מהראש של אלעד את עומס תכנון-הארוחות | ❌ Phase 2 לא נבנה |
| "מה להפשיר הערב?" בקול/וואטסאפ בלי לפתוח UI | ❌ הצינור קיים בצד-האפליקציה בלבד |
| שתי דלתות-כניסה: UI + שכבת-סוכן | 🟡 UI מצוין; דלת-הסוכן בנויה אך לא מחוברת |
| נראוּת לעבודה שאלעד עושה ("העבודה שלי לא נראית") | 🟡 מד-ההוגנות בנוי — אך בלי שימוש אין נתונים |

**מסקנה demand-over-vanity:** אסור לבנות עוד פיצ'רים לפני שהבית עצמו משתמש.
הצעד הבא הוא לא קוד-מוצר — הוא **חיבור + התחלה-נקייה + הרגל**.

---

## 4. כוכב-הצפון של התוכנית

> **14 ימים רצופים שבהם אלעד וענבל מקבלים ערך יומי מבית-בסדר בלי לפתוח אותו** —
> דרך וואטסאפ (שם המשפחה חיה) — ולפחות רגע-קונפליקט אחד ("לא הפשרת!") שנמנע
> כי הסוכן הזכיר בזמן.

רק אחרי שזה קורה — מדברים על משפחות אחרות, תשלומים וחלומות-מוצר.
העיקרון: **בית-בסדר הופך מ"אפליקציה שצריך לזכור לפתוח" ל"נוכחות שקטה בוואטסאפ".**

### שלושת המהלכים שמשנים הכל
1. **החוט החסר — לחבר את קאמי באמת** (~2-3 שעות עבודה): מפתח + כלי בצד-קאמי →
   "מה יש היום בבית?" / "תוסיף משימה" / "סיימתי את הכביסה" עובדים מוואטסאפ.
2. **מתכנן-הארוחות** — הרוצח-האמיתי-של-הכאב. הכל מעוצב; ה-intake של 5 השאלות
   ייאסף **בשיחת-וואטסאפ של 5 דקות דרך קאמי** (לא טופס שאלעד לעולם לא ימלא) —
   ועד אז נבנה עם **סבב-טיוטה** שאלעד רק מתקן.
3. **התחלה-נקייה + brief חומל**: לארכב את 43 החובות, ולתקן את הטון — brief יומי
   שפותח ב"מה אפשר לעשות היום" ולא ב"⚠️ 43 באיחור" (קיר-בושה = הסיבה שאפליקציות
   כאלו ננטשות; מנוגד גם לפילוסופיית מד-ההוגנות הלא-מבייש שלנו).

---

## 5. אופק א' — ביצוע אוטונומי 48 שעות (בלי אלעד, בלי סיכון)

> כללים: כל שינוי-קוד דרך branch → Vercel preview (safe-live-refactor) — **לא ישר ל-master**.
> אפס הודעות יוצאות. שינויי-דאטה בפרוד = סקריפט מוכן + dry-run, ההרצה = טאפ של אלעד.
> כל צעד מסתיים בהוכחה (curl / בדיקה / צילום).

### א. אבחון-אמת של הקרונים (שעה)
- לוודא ש-`CRON_SECRET` קיים ב-Vercel (דרך `vercel env ls` אם יש CLI מחובר; אחרת
  לתעד כטאפ-של-אלעד). לבדוק ב-Vercel logs אם daily-brief רץ ב-08:00 ומה הוא שלח.
- אם ה-brief כן נשלח יומית עם "43 באיחור" — זו כנראה סיבת-הנטישה עצמה. לתעד.

### ב. חיווט קאמי → בית-בסדר (הלב של 48 השעות; ~3 שעות)
1. להוסיף `BAYIT_AGENT_KEY` + `BAYIT_HOUSEHOLD_ID` לסביבת קאמי ב-Contabo
   (המפתח כבר קיים ב-.env.local של הפרויקט; ה-UUID של משק-הבית — שאילתה אחת ב-Supabase).
2. כלי חדש אחד בקאמי (בהתאם ל-Action Contract שלו): `bayit(action, params)` שממפה ל-
   `GET /api/agent/brief?householdId=…` · `POST /api/agent/task {list|add|complete}` ·
   `POST /api/agent/plan`. ה-API כבר מחזיר `whatsappText` מוכן — קאמי רק מעביר.
3. שורת-הנחיה בפרומפט של קאמי: זיהוי כוונות-בית בעברית ("מה יש היום בבית", "תוסיף
   משימה", "סיימתי את X", "מה להפשיר") → קריאת הכלי. **בלי הודעות יזומות עדיין.**
4. בדיקת-קצה: הודעת-בדיקה של אלעד עצמו לקאמי → תשובה עם נתוני-brief אמיתיים.
   (עד אז — לאמת עם curl מה-VPS שהכלי עובד.)
- גיבוי לפני כל שינוי בקאמי + `kaylee-smoke` לא רלוונטי כאן, אבל restart מבוקר + לוג.

### ג. התחלה-נקייה — סקריפט מוכן-להרצה (שעה)
- `scripts/fresh-start.mjs`: מארכב (status→archived/skipped, לא מוחק!) את כל המשימות
  שעברו את מועדן לפני 1.7.2026, מאפס streaks בעדינות, ומדפיס סיכום. כולל `--dry-run`.
- **לא מריצים על פרוד בלי אישור-אלעד** — אבל הסקריפט + פלט ה-dry-run מוכנים לטאפ.

### ד. brief חומל (branch + preview בלבד)
- `daily-brief` + `/api/agent/brief`: תקרת-תצוגה לאיחורים ("יש כמה משימות ותיקות —
  רוצה התחלה נקייה?" במקום ⚠️ 43), פתיחה ב-1-3 משימות של היום, שורת-הוגנות שבועית
  ביום שישי. שינוי קטן, טון = הכל.

### ה. טיוטת הזמנת-ענבל + תסריט השקה-ביתית (חצי שעה)
- לשחזר את `docs/LAUNCH-HOME.md`: הודעת-וואטסאפ קצרה ואנושית לענבל (בקול-אלעד,
  לפי elad-voice; **טיוטה בלבד — אלעד שולח בעצמו**), + "טקס-התחלה" של 10 דקות ביחד:
  בוחרים 5 משימות אמיתיות לשבוע, כל אחד מסמן אחת שהשני עשה.

### ו. היגיינה (חצי שעה)
- להסגר `supabase/migrations/001_initial_schema.sql` → `docs/archive/` עם README-אזהרה.
- למחוק סקריפטים חד-פעמיים (`try-migration.mjs` וכו').
- לעדכן PROGRESS.md + לוודא tsc/build/tests ירוקים אחרי כל שינוי.

### ז. טיוטת מתכנן-הארוחות (אם נשאר זמן — הכנה בלבד, מאחורי feature-flag)
- מיגרציה `015_meals.sql` (meals, meal_plan לפי הספק ב-AGENT-INTERFACE.md) — קובץ בלבד,
  לא מורץ. + `GET /api/agent/prep` (סטאב שמחזיר 501 עד שיש נתונים).
- טיוטת-rotation של 8 ארוחות מנוסחת כהצעה שאלעד רק מתקן בוואטסאפ ("תמחק/תוסיף/תקן").

**הוכחת-סיום של 48 השעות:** צילום שיחת-וואטסאפ שבה קאמי עונה על "מה יש היום בבית?"
עם נתונים אמיתיים + branch ירוק ב-preview + סקריפט fresh-start עם dry-run מתועד.

---

## 6. אופק ב' — המשך ליורש (שבועות 1-6 ואילך)

### שבוע 1 — ההשקה-הביתית (דורש ~30 דקות של אלעד)
| טאפ של אלעד | מה זה פותח |
|---|---|
| 5 תשובות-intake בוואטסאפ לקאמי (5 דק') | מתכנן-הארוחות נבנה על אמת |
| אישור הרצת fresh-start (1 דק') | לוח נקי, בלי קיר-בושה |
| שליחת ההזמנה לענבל + טקס 10 דק' ביחד | המשתמשת השנייה |
| (אופציונלי) הרצת מיגרציה 014 על branch | סגירת חוב-DB ותיק |

### שבועות 2-4 — לולאת-ההרגל (היורש עובד, אלעד חי)
- **21:00 כל ערב:** nudge הפשרה/הכנה ("מחר קציצות — תוציא עוף מהמקפיא") דרך קאמי.
  זו הפעולה בעלת ה-ROI הגבוה ביותר בכל הפרויקט — היא מפרקת את רגע-הקונפליקט המקורי.
- **08:00:** brief חומל (3 משימות היום, מי לוקח מה).
- **שישי:** שורת-הוגנות שבועית (מד-ההוגנות סוף-סוף מקבל נתונים אמיתיים).
- reply-to-complete מוואטסאפ — לאמת E2E (הקוד קיים; חסר אימות חי).
- מדידה: יומן-שימוש פשוט (ימים עם ≥1 השלמה מכל בן-זוג; nudge שנענה; דיווח שבועי לאלעד).
- **כלל-עצירה:** אם אחרי שבועיים אין שימוש — לא לבנות עוד. לשאול את אלעד וענבל *למה*
  (ראיון 10 דקות), לתקן את הדבר האחד שחוסם, לנסות שוב. איטרציה, לא ערימת-פיצ'רים.

### שבועות 5-6 — רק אם הבית משתמש (שער-כניסה קשיח)
- גיימיפיקציה עדינה לילדים (lib/pets.ts כבר מוכן — 20 חיות, נפתחות ברצפים) — אם רלוונטי לבית.
- הרחבת intent-ים בקאמי (קניות: "תוסיף חלב לרשימה").
- MCP-server דק מעל ה-Agent API (המניפסט `capabilities` כבר בנוי כמעט-MCP) — מאפשר לכל
  סשן-Claude של אלעד לתפעל את הבית.

### אופק-3 — משפחות אחרות (רק אחרי 30 ימי-שימוש-בית אמיתיים)
- ההוכחה השיווקית היחידה ששווה משהו: "אנחנו חיים עם זה חודש". פוסט-אלעד (לפי elad-post-rules)
  עם הסיפור האמיתי → waitlist → 5 זוגות-בטא בחינם → רק אז מונטיזציה.
- תיקון Sumit: לפורט את פיקס ה-`/creditguy/gateway/` המוכח מ-PDF-Empire (1.7) + לוודא
  vendoring של `_lib/sumit-client` ל-Vercel + `SUMIT_WEBHOOK_SECRET` + סיבוב-טוקן.
- **לא לפני כן.** (demand-over-vanity: אין ביקוש מוכח = אין בניית-גבייה.)

---

## 7. קווים אדומים (לכל יורש)

1. **אפס הודעות יוצאות לענבל או לכל אדם** בלי אישור מפורש של אלעד. תמיד טיוטה.
2. **טון לא-מבייש** — בשום הודעה, brief או פיצ'ר. בלי "⚠️ נכשלת", בלי השוואות מלחיצות.
   זה DNA-המוצר (מד-ההוגנות תוכנן כך בכוונה) וזה גם התנאי שענבל תישאר.
3. **שינויי-פרוד** רק דרך branch→preview→אימות (safe-live-refactor). מיגרציות — branch קודם.
4. **לא למחוק דאטה** — רק לארכב. ההיסטוריה של הבית שייכת לבית.
5. **reuse-first**: לפני כל בנייה — לקרוא `docs/AGENT-INTERFACE.md`, `docs/AGENT-API.md`,
   `PROGRESS.md`. כמעט הכל כבר מעוצב; אל תמציא מחדש.
6. שינוי בקאמי = לפי הפרוטוקול שלו (גיבוי → פאץ' → smoke). קאמי הוא **הוא** (זכר).
7. הפרויקט **לא תופס משבצת WIP=3** — הוא תשתית-חיים-אישית, מתקדם במנות קטנות (48ש' + טאפים),
  לא ספרינט שדוחק את Triplus/PDF/הספר.

---

## 8. קבצים ומקורות-אמת

| מה | איפה |
|---|---|
| החזון המקורי + מודל מתכנן-הארוחות + ה-intake | `docs/AGENT-INTERFACE.md` |
| חוזה ה-Agent API (auth, endpoints, gotchas) | `docs/AGENT-API.md` |
| scorecard מוכנות-להשקה | `docs/LAUNCH-READINESS-2026-06-14.md` |
| יומן-התקדמות מלא | `PROGRESS.md` |
| קרונים | `vercel.json` |
| זיכרון-רשת: הקשר הרגיש של ה"למה" | `memory/project_bayit_agent_interface.md` (גלובלי) |
| פרוד | https://www.bayitbeseder.com · Vercel auto-deploy מ-master |
| Supabase | פרויקט `uqumzjmyejlhoyliyesu` (ייעודי, RLS פעיל) |

*נכתב 4.7.2026 · צלילה + תכנון בלבד · שום דבר לא נפרס ושום הודעה לא נשלחה.*
